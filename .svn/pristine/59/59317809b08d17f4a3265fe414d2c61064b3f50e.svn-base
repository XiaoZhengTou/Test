define(['angular'], function (angular) {
    var currency = angular.module('currency', []);
    currency.controller('currencyCtrl',['$scope',function($scope){

   }]);
   angular._LoadModule('currency');
   return currency;
});