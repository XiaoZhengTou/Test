define(['angular', 'js/templates/financeQC/analogSampled.js'], function(angular) {
	publicFunction.addStyle('md-data-table.min.css');
	publicFunction.addStyle('airfare.css');
	publicFunction.addStyle('task.css');
	var app = angular.module('financeQC', []);
	app.config(function($routeProvider) {
		$routeProvider.when('/financeQC/analogSampled', {
				templateUrl: 'templates/financeQC/analogSampled.html'
			}).otherwise({
				redirectTo: 'templates/financeQC/planManagement.html' 
			});
	});
	app.controller('editPlanManagementCtrl', ['$scope', '$http', '$filter', '$mdDialog', function($scope, $http, filter, $mdDialog){
//		alert(1255);
		
	}]);

	angular._LoadModule('financeQC');
	return app;
});